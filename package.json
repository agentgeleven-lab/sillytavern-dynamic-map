{
  "name": "sillytavern-dynamic-map",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test", "check": "node scripts/check.mjs" },
  "engines": { "node": ">=18" }
}
