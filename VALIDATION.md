# v0.10.1 验证记录

- 结构/语法检查：43 个 JavaScript 模块、相对导入文件、JSON、扩展入口。
- 79 项自动测试：包括原有隔离和保存检查、新拖动自由放置/任意邻居吸附/单向道路保留/重叠避让、DOMPoint 坐标兼容、道路目录迁移、世界书范围与禁用条目过滤、界面偏好独立存储。
- 独立 Edge 无头浏览器执行实际 pointerdown/move/up：拖动时跟随指针，松手后按方位吸附；远处自由放置保留准确坐标并断路；未保存时查看页不变化，保存后生效。
- 浏览器检查：简洁按钮切换、道路类型下拉框及规则内新增类型、消息末尾按钮挂载/开关/打开地图/不修改正文、浅色主题立即切换并在刷新后保留。
- 模拟模型输入检查：默认传入角色卡和绑定书，开启全局选项后只增加已开启全局书；未读取列表中的未开启书。
- 浏览器用模拟角色卡、世界书和生成接口运行，没有调用真实模型。真实 SillyTavern/TauriTavern 的主题兼容、元数据网络保存、模型生成以及受管理消息表面仍需实机验证。
- 小白X、外部状态栏自动联动及 Tool Calling 未实现。

- 独立 API 回归：地址标准化、会话密钥/选择记忆/清除、配置与聊天隔离、HTTP 失败不回显原始正文、空响应/截断/网络异常/取消处理。
- 浏览器真实 HTTP 请求指向本地模拟服务，验证独立模型在缺少酒馆 generateRaw 时正常生成草稿、传入角色和已开启全局书、未保存不影响查看页、密钥不写入本地常规配置且刷新后清空。未调用真实外部服务或付费模型。


- 消息内地图回归：按钮在消息下方创建相对定位的六页窗口，不展开悬浮窗；消息内编辑只有保存后进入已保存地图，关闭重开保留共享草稿；关闭入口时移除窗口，消息正文保持原样。

- 永不结束的读取任务/酒馆模型 Promise：总超时及取消均释放等待；迟到结果不会转为成功。
- 浏览器复现酒馆模型一直 pending：取消后立即恢复生成按钮；实际等待 10 秒后总超时恢复按钮，已保存地图不变。地图名称编辑、保存和查看一致。
- 中性协议样例不含旧地图地理名称；原有 44 项回归继续通过。先前修复的是无限等待和反馈缺失；用户已报告空回复问题解决。

道路距离更新：通过可选距离、旧距离迁移、空名称显示、拆分总距离不变及距离不影响图上长度的测试。完整浏览器模拟回归通过，未调用真实模型。

新增模式：检查旧地图保留、重复及冲突拒绝、30 条聊天范围、距离选项；真实模型效果尚未验证。

自动布局：三角闭环、密集图、23 分支同点起始、锁定方位、固定坐标、矛盾约束原子拒绝、多组件、空地图、增量锚点及规模上限测试通过。浏览器验证手动添加道路按钮、闭环一键整理、未保存隔离、保存坐标不回退、固定地点后再次整理位置不变。没有为本次布局调用真实模型。

250 地点链式地图布局约 1.1 秒（本机单次测量，不代表所有设备）；补充大图最终避让回归。

六边形/方格：负坐标往返、吸附、占格与固定冲突、距离和道路连接不变、增量旧点保留、异常原子拒绝。层级：入口与循环检查，AI 不覆盖兄弟及父地图，缺失入口拒绝。浏览器实际操作验证形态转换、未保存隔离、创建城市子地图、方格新增地点与拖动、保存、返回父地图与重新进入、刷新保留。全部通过；模型和宿主仍为本地模拟。

格子属性：批量绘制与节点/道路独立、移动不带走属性、稀疏清除、导入往返、行政路径与循环、使用中条目删除保护、非法坐标及颜色原子拒绝、AI 保留人工涂色通过。浏览器验证点击格子、连续刷选、批量应用、未保存隔离、创建行政区域、关联格子、查看说明与刷新保留；真实宿主和真实模型仍未验证。

统一导航和生成：祖先入口投影、完整位置路径、六类独立提示词、生成范围/父级/入口资料、草稿撤销及后续编辑失效、显式角色地图冲突通过。浏览器验证浏览前后已保存文档完全相同、父地图当前位置标记、地点内部入口、按层级新建子地图、生成撤销、全部保存、原地图与角色位置保留、生成中切换地图拒绝迟到结果。使用本地模拟模型，无真实付费请求。

查看页入口回归：点击地点前无子地图进入按钮；点击关联地点后出现详情入口，点击进入后仍保留角色位置。浏览器完整回归通过。
