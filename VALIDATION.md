# 验证记录 · 0.3.0 · 2026-09-08

- 17 个 JS 模块的语法、相对引用、JSON 和 manifest 入口检查通过。
- 16 项测试通过：原有 8 项核心测试；另含聊天 A/B 隔离、事件前旧绑定拒绝、延迟保存、切换期间导入拒绝、损坏数据保护、删除清理引用、失败恢复、同名新聊天不复活已同步缓存。
- 独立浏览器模拟：聊天 A 新增龙门港和港口路后保存；聊天 B 仍是三地点；切回 A 和刷新均恢复四地点及新路线。
- 指针拖动节点和空白画布后位置改变；滚轮缩放显示 89%，保存状态正常。点击拖动后的节点可显示详情。
- 无效版本 JSON 导入被拒绝，原地图保留；有效文档在确认后成功替换为「导入验证地图」并显示保存完成。导入前代码触发备份下载，未逐字节检查下载文件。
- 实际酒馆的保存、群聊、聊天改名、多用户环境、移动端触摸和主题兼容性尚未实测。浏览器演示使用模拟上下文，不能等同酒馆实测。
